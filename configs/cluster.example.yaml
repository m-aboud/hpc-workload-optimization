cluster:
  name: demo-hpc
  scheduler: slurm
  default_partition: compute
  gpu_partition: gpu
storage:
  scratch: /lustre/scratch
  project: /project
fabric:
  type: infiniband
  mpi: openmpi
operations:
  approved_benchmark_partition: benchmark
  max_test_nodes: 2
